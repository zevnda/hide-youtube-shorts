{
    "type": "module",
    "version": "1.8.0",
    "scripts": {
        "compile": "tsc",
        "build": "pnpm compile && node build/build.js",
        "dev": "tsc --watch"
    },
    "devDependencies": {
        "@types/chrome": "^0.0.246",
        "@types/node": "^20.0.0",
        "@types/webextension-polyfill": "^0.10.0",
        "typescript": "^5.0.0"
    }
}